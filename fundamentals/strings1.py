# Strings are sequences of characters, 
# and they are immutable (cannot be changed after they are created)


# my_name = input("Enter your name: ")
# greeting = "Hello, " + my_name + "!"
# print(greeting)

string1 = "Hello"
string2 = "World"

print(string1 + " " + string2)

print("sarath" * 3)
my_int = 10
print(my_int)

my_int = "hello world"
print(my_int)

# string is a sequence of characters, so we can use indexing and slicing to access parts of the string
print(string1[0])  # access the first character

# reverse the string using slicing
print(string1[::-1])